import React from "react";

const OtherAdviceButton = ({ loadAdvice }) => (
	<button className="Advice-other-advice-button" onClick={loadAdvice}>
		Show other advice
	</button>
);

export default OtherAdviceButton;
